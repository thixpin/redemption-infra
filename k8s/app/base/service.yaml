apiVersion: v1
kind: Service
metadata:
  name: redemption-api
  namespace: redemption
  labels:
    app: redemption-api
spec:
  type: ClusterIP
  selector:
    app: redemption-api
  ports:
    - name: http
      port: 80
      targetPort: http
