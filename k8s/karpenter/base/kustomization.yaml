apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base carries dev values; the prod overlay patches the env-specific fields
# (node IAM role name + karpenter.sh/discovery tags = "<project>-<environment>").
resources:
  - nodepool.yaml
  - ec2nodeclass.yaml
